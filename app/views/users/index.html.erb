<div class="row">
<div class=col-md-3>
</div>
  <div class="col-md-6">

<p id="notice"><%= notice %></p>

<h1>Utilisateurs</h1>

<table class="table">
  <thead>
    <tr>
      <th scope="col">Prénom</th>
      <th scope="col">Nom</th>
      <th scope="col">E-mail</th>
      <th scope="col" colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td class="align-baseline"><%= user.first_name %></td>
        <td class="align-baseline"><%= user.last_name %></td>
        <td class="align-baseline"><%= user.email %></td>
        <td class="align-baseline">

          <!--<div class="btn-group" role="group">
          <button type="button" class="btn btn-default"><%= link_to 'Infos', user %></button>
          <button type="button" class="btn btn-default"><%= link_to 'Editer', edit_user_path(user) %></button>
          <button type="button" class="btn btn-default"><%= link_to 'Supprimer', user, method: :delete, data: { confirm: 'Êtes-vous sûr ?' } %></button> -->
        </div>
        </td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>
